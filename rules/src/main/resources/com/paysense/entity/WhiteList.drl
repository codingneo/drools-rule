package com.paysense.entity;

global java.util.List whileList;

rule "Extract New Transaction"
	when
		TranObjectContainer(newTransaction.status == 0, $tran:newTransaction, $user:user)
	then
		System.out.println( "**************testing**************: "+$user );
		$tran.setStatus(1);
end